# keepcoding_ciberseguridad

Antes de lanzar ningun script:
1. Crear entorno virtual con "python -m venv .venv"
2. Activarlo "source .venv/bin/activate"
3. Instalar los paquetes que se han utilizado "pip install -r requirements.txt"